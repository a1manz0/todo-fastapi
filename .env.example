# Database configuration
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
POSTGRES_USER=todos_user
POSTGRES_PASSWORD=todos_password
POSTGRES_DB=todos

# Database connection parameters (опционально, для тестов)
DB_HOST=db
DB_PORT=5432

# Optional: Uncomment and modify for production
# HOST=0.0.0.0
# PORT=8000
# CORS_ORIGINS=http://localhost:8080,http://your-domain.com 